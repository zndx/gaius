# Brand packs (logo only)

Theme stays **Keiretsu**. Product identity stays **Gaius**. Packs supply
logo / mark / favicon only.

| Pack | Default | Source |
|------|---------|--------|
| `weathership` | yes (`GAIUS_UI_BRAND`) | `wxs/site/brand` lockup mono-white |
| `cloudera` | | Atelier Cloudera wordmark |
| `custom` | empty `+` until upload | operator `.tgz` (Weathership kit layout) |

See each pack’s `brand.json`. Custom uploads land under
`build/dev/.gaius-ui-brand/custom/` (not this tree).
