# Default Profile Configuration
# =============================
# Inherits from base.conf (merged at load time)
# Only specify values that differ from base

gaius {
  profile = "default"

  # Default profile uses all base settings
  # No startup commands
  startup {
    commands = []
    show_situational = true
  }
}
