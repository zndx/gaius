# Gaius postgres on contract :5444

**Date:** 2026-08-13

Orphan postmaster (session-1.scope, ~10h) held `.devenv/state/postgres` on
**:5445** (devenv port shift). Unit postgres `gave_up` on the lock. Engine
Status was up without a DB.

Stop now SIGTERM/KILLs a leftover Gaius postmaster. Start is not ready
until `pg_isready :5444`. Unit `PGPORT=5444`. Engine `wait_for_postgres`.

Verified: `zndx_gaius` on :5444, engine in `system.slice/gaius.service`,
no :5445.
