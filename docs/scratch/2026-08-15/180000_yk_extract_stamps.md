# YK resource-class stamps for article_curate / prospects_update

No `root.gaius`. Both flows admit a YK Application on
`root.internal.inference.extract` with `federation.zndx.org/gpu: 1` (not
`nvidia.com/gpu`). Project stays a label. Admit (pod Running) before Metaflow
spawn; `BeginWorkload` fail-fasts `#YK.00000003.NOAPP` without a live claim.
Yield still SIGTERMs the host child via last-gasp → Engine/Yield.
