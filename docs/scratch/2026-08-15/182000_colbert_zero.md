# ColBERT-Zero replaces ColPali

Late-interaction SoR is `lightonai/ColBERT-Zero` (Apache 2.0, public
Nomic-embed mix, PyLate). Collection `gaius_kb_colbert_zero`. Text only;
images `#EM.00000001.NOVISION`. ColNomic index is not compatible — reindex.
Removes `colpali-engine` so transformers 5.x can stay for Qwen3.8.
