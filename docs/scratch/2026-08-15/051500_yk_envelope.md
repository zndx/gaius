# YK envelope for always-on survey

Signals dashboard should show one Running extract Application (1/6 GPU).
Gaius self-caps: 1 app, 1 `federation.zndx.org/gpu`, 16Mi, 10m. Prospects
and articles share that id. `#YK.00000004.ENVELOPE` if a second claim
appears. Parent `internal.inference` max GPU=6 is the physical lid.

Host floors (YK cannot see these): `/` ≥32Gi free, `/raid` ≥64Gi,
`MemAvailable` ≥8Gi. `#YK.00000005.DISK` / `#YK.00000006.MEM` refuse
the child and leave the Application up. Measure disk with `dust`.
