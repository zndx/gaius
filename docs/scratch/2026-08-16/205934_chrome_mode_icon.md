# Chrome light/dark is a sun/moon icon

Mode toggle no longer writes Light/Dark into the button (that would
wipe an SVG). Sun in dark mode, moon in light; title is “Switch to
…”. Asset `0.2.11-mode-icon`.
