# Weathership default + Settings brand packs

Cloudera mark was leftover chrome. gaius-ui now matches Signals:

- Default pack **weathership** (mono-white lockup, dark chrome)
- `/settings` gear; deployment-wide `build/dev/.gaius-ui.json`
- Third card **custom** is empty `+` until a `.tgz` is ingested
- Archive: flat `logo.svg`+`favicon.svg` or `wxs/site/brand` kit
  (`logo/lockup/lockup-mono-white.svg`)

Product name stays Gaius. Theme stays Keiretsu.
