# Agenda nav placeholder

Top nav: **Agenda · Cognition · Terminal**. Board stays implicit at `/`.

`GET /agenda` is a landing-hero placeholder (KB day planner /
`current/agenda.md`). No web list yet — CLI and grok `/agenda`
already hit Engine/ThetaAgenda.
