# Agenda intents: brief, reminder, session

The Agenda is the calendar. Cards carry `intent` plus shape
(`note`/`list`/`event`). Sessions require `starts`; body is the brief
and a Google Calendar TEMPLATE link (iPad Safari / Calendar.app).

Nine surface cards rewritten in place. ACP `/agenda` now points at
`gaius__agenda_list` / `create`. Live emit: filings → session,
errors → reminder, publish>0 → brief.

Perseids: not in `content_items` or X bookmarks, so not filled.
Guru `#AG.00000006.NOMEETTIME`, `#AG.00000007.BADINTENT`.
Asset `0.2.12-agenda-intent`.
