# Ask panel

Native Keiretsu right rail (not Metabot, not AGPL). Chrome toggle
Ctrl+E. Engine/Complete thinking + markdown. `GaiusSurface` tells
the model what the UI is looking at — Agenda focus body/times so
"when should I leave for this flight" uses the open event.
