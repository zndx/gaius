# Agenda first pass

Consciousness surface: scratch zettels (`note` | `list` | `event`)
named `scratch/YYYY-MM-DD/YYYY-MM-DD-HHmmss_<skewer>.md` with
`prev:`/`next:`. Engine RPCs AgendaList/Get/Create/Update. Web
`/agenda` is Keep-board + Schedule. MCP `agenda_*`. ThetaAgenda
list|init still owns `current/agenda.md`.
