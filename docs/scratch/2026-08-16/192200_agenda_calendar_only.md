# Agenda is the calendar

Board and the Board/Schedule toggle are gone. One recency-aligned
calendar: events on `starts`, notes and lists on `created_ms`.
Days newest-first; within a day, clock order. Pin still floats a
row inside its day. FAB / editor / prev-next unchanged.
