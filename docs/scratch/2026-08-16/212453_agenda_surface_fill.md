# Agenda back+forward fill from live clocks

Nine zettels from what actually ran 13–16 Aug (and the next 13F
date). No Perseids (absent from FMP/HN). No invented 13F holder
deltas (`institutional-ownership` is 402; cache empty). Dodge &
Cox / State Street are SC 13G already in the CHTR KB.

Live emit: `agenda_emit` after `prospects_update` and non-zero
`publish_cards`. Needs an engine recycle to attach. Guru
`#AG.00000005.EMITFAIL` is fail-open on the task.
