# Discover landing P0

`/` is Discover (ELK query + hist + Activation Examples table).
Board grid lives at `/lab/board`. Watts left the chrome (`/gpu watts` remains).

Engine `DiscoverSurface` reads `content_items` last window. Feature pins
with no tape return empty docs. CLI: `/discover`, `/discover 7d source:arxiv_cs_dc`.
