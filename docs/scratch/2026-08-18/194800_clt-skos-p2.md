# P2 ACP SKOS alignment

`skos_alignment` ledger + `CltSkosEvalFlow.acp_align`. Ambiguous =
one CLT feature on items with more than one aperture code. Max 3 ACP
prompts/run. No GitHub issues. First live call: Mistral 402 →
`verdict=pending` (honest, not a silent skip).
