# Summary lineup (FedWiki, temporal)

SECTION = Ontology / Heuristic. LENS = Articles / Projects / Thoughts.
Default seed is this ISO week's Signals Summary. Hop is `SummaryHop`;
fork writes scratch with origin (peer via `ServerQuery` NOTE).
`/summary` sits between Cognition and Terminal.
