# ServerQuery (Matrix S2S Queries, not gossip)

`zndx.engine.v1.Engine/ServerQuery` — pairwise snapshot. Remotes are
named (`origin` / `internal` / `share` here). Live on `:50051`:
origin=/raid/repos/zndx-gaius, share=github.com:zndx/gaius,
internal=gaius-acp. Guru `#SS.00000001–02`.
