# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
#
# Ports: devenv assigns PGPORT so worktrees and sibling projects do not
# collide. Do not pin 5444 from systemd. Lattice *prefers* 5444
# (services.postgres.port) when free.
#
# Secrets: DECLARED in secretspec.toml, PROVIDED per environment.
# dotenv (.env) is the local default (gitignored).
#   devenv --secretspec-provider env shell
#   secretspec run -- uv run gaius
secretspec:
  enable: true
  provider: dotenv
  profile: default

inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling
  blender-bin:
    url: github:edolstra/nix-warez?dir=blender

# If you're using non-OSS software, you can set allowUnfree to true.
# allowUnfree: true

# If you're willing to use a package that's vulnerable
# permittedInsecurePackages:
#  - "openssl-1.1.1w"

# If you have more than one devenv you can merge them
#imports:
# - ./backend
